import React, { Component } from "react"
import ReactDOM from "react-dom"
import App from './App'

ReactDOM.render(<App />, document.getElementById("root"))
// render and attach the element into the 'root' 😄 